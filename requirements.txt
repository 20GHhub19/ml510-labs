# Install from the repository root: python -m pip install -r requirements.txt
# Shared environment for all labs; CPU execution is the default.
# Direct package pins, not a complete transitive lockfile.
numpy==2.2.6
scipy==1.15.3
pandas==2.2.3
scikit-learn==1.7.2
seaborn==0.13.2
matplotlib==3.10.6
tensorflow==2.20.0
keras==3.11.3
jupyterlab==4.4.7
ipykernel==6.30.1
nbclient==0.10.2
nbformat==5.10.4

# Install the local course packages in editable mode.
-e .
